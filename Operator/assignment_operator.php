<?php

 // Assignment Operators (+=,-=,*=,/=,%=) (নিয়োগ দেয়া)
        
    // All variables
        $plus = 20;
        $mynus = 25;
        $into = 30;
        $devide = 35;
        $percent = 40;

    // Assign variables
        $plus += 5; 
        $mynus -= 10; 
        $into *= 15; 
        $devide /= 20; 
        $percent %= 25; 

    // Show
        echo $plus . '<br>';
        echo $mynus . '<br>';
        echo $into . '<br>';
        echo $devide . '<br>';
        echo $percent . '<br>';

?>